{% if image is not defined %}

  <figure class="{{ figure_class|join(' ') }}">
    <img class="lazy" src="https://placehold.it/20x10" data-src="https://placehold.it/2000x1000" alt="placeholder">
  </figure>

{% else %}

  <figure class="{{ figure_class|join(' ') }}">
    <img{{ image|get_timber_image_responsive(size) }}>
  </figure>

{% endif %}
